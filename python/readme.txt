Installation:

- from the python directory run pip install -r requirements.txt

Run:

- from the python directory run py main.py

Output:

- generated HTML file shares.html in the python/output folder